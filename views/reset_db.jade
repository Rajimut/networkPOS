extends layout

block holder
  .row
    pre
      h3.text-center.text-danger Selecting reset will destroy all data and load default information
    .col-md-6
      h4 for sellers:
      | 
      em target@mushrooms.com, walmart@mushrooms.com, starbucks@mushrooms.com, ruggles@mushrooms.com
        h4 Password: seller
    .col-md-6
      h4 for buyers:
      | 
      em buyer1@mushrooms.com, buyer2@mushrooms.com, buyer3@mushrooms.com, buyer4@mushromms.com, buyer5@mushrooms.com
        h4 Password: buyer

  .panel.panel-default
    .panel-body
      .row
        .center-block
          .col-md-6.col-md-offset-3
            button.btn.btn-danger.btn-lg.btn-block(data-action='seller_db') Reset seller db
            button.btn.btn-danger.btn-lg.btn-block(data-action='buyer_db') Reset buyer db
            button.btn.btn-danger.btn-lg.btn-block.hidden(data-action='items_db') Reset items db
            button.btn.btn-danger.btn-lg.btn-block(data-action='invoice_db') Reset invoice db
            button.btn.btn-danger.btn-lg.btn-block.hidden(data-action='reset_all') Reset All db
  script(type='text/javascript').
    $('.btn').click(function(event) {
    event.preventDefault();
    console.log($(this).attr('data-action'));
    var _post_req = $.ajax({
    url: '/reset',
    type: 'POST',
    data   : JSON.stringify({action: $(this).attr('data-action')}), //EXPECTING JSON TO BE RETURNED
    contentType: 'application/json; charset=UTF-8'
    });

    _post_req.success(function(data){

    //- $('#receipt_no').text(data['receipt_no']);
    //- $('#transaction_btn').text('Post Bill'); //  CHANGE BUTTON NAME
    //- $('#transaction_btn').attr("disabled",false); //  CHANGE BUTTON NAME
    //- $('#trans_complete [name]').prop('disabled',false);
    //- $('[id^=delete_row], [id^=add_row]').attr("disabled",false);
    //- $('#new_receipt').attr('disabled',true);
    //- $('#print_receipt').attr('disabled',true);
    //- $('[id^=addr]:not(:first)').remove(); // REMOVE ALL OTHER NEW ROWS

    //- $('#trans_complete').find('input:text, input:password, input:file, textarea').val(''); // RESET FORM
    //- $('#trans_complete').find('input:radio, input:checkbox').removeAttr('checked').removeAttr('selected');   // RESET FORM   
    //- $('div[name]').each(function(){
    //-     $(this).text($(this).attr('value')); //RESET FORM
    //-   });

     })

    //window.location.reload(true);


     });